/**
 * Признаки окружения: чем ввод устройства отличается от мыши и трогал ли пользователь прокрутку.
 *
 * Отдельным входом (`@brandup/ui-kit/env`), потому что это нужно уже базе ввода — а ей нельзя
 * тянуть общий вход кита: тот приносит попап, модальное окно, стили и `@brandup/ui-app`.
 * Раньше она лезла прямо в файлы кита (`@brandup/ui-kit/source/utils/...`), и путь держался
 * на том, что пакет отдаёт наружу всё подряд.
 *
 * В отличие от {@link ./names} побочный эффект здесь есть: `user-scroll` заводит слушатели
 * жестов прокрутки прямо при загрузке — иначе первый же жест до подписки был бы упущен.
 */
export { IS_TOUCH_DEVICE, isCoarsePointer } from "./utils/compatibility";
export { hasUserScrolled, resetUserScroll } from "./utils/user-scroll";
